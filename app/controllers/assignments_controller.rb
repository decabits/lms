class AssignmentsController < ApplicationController

  def show
    # @track = Track.includes(:track_items).find(params[:id])
    # @track_item = TrackItem.includes(:assignments).find(params[:track_id])
  end

end
